#include <stdio.h>
int main(){
    int t;
    int n;
    scanf("%d", &t);
    for(int i = 0; i < t; i++){
       scanf("%d",&n);
     for(int m = 0; m <= n; m++){
        int k;
        for(int k =1; k <= m; k++){
            if(int k)
            printf("%d\n",k);
        }
     }
    }

    return 0;
}